[project]
name = "nsx-api-sh"
version = "0.1.0"
description = "A modern, modular, and interactive Bash toolkit for managing VMware NSX-T via its REST API."
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "requests>=2.32.4",
    "rich>=14.0.0",
]
